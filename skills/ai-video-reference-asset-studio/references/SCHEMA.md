# Reference Asset Knowledge Schema

Enter this knowledge layer only at the first asset-production judgment. Read this file, `index.md`, and recent `log.md`; choose the matching route under `queries/`; read only its required topic pages; produce the node output; then return to `SKILL.md` step 3.

Source classes: `W` compiled knowledge, `U` user-confirmed requirements, `E` verified source/runtime evidence, and `A` provisional synthesis. `source-manifest.md` is maintenance provenance, not a runtime reading list.
