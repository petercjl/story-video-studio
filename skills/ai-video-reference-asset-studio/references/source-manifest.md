# Source manifest

- `U`: the user approved one canonical reference-asset production Skill driven primarily by video-prompt reference requests.
- `U`: exact asset IDs, generation evidence, approved versions, and downstream-resolvable image paths must remain traceable.
- `U`: one logical asset may require several independent images or one intentional board; selection depends on whether identity, resolution, approval, and authority can safely be shared.
- `U`: failed and derivative character images never replace the original approved identity source.
- `U`: formal keyframes begin with the upstream requested frame. Additional control frames require evidence from a real video test and a reviewed revision.
- `E`: the current Codex and SealSeek media adapters define provider-specific generation and artifact delivery.

Recompile this knowledge when the reference-request major version, package schema, source-authority policy, or tested provider behavior changes materially.
